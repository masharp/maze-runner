/*
    Insired by https://www.youtube.com/watch?v=rop0W4QDOUI
    - by Michael Sharp 2017
    www.softwareontheshore.com
*/
'use strict';
const imageGenerationAPI = 'http://localhost:3000/api/maze';
const analysis = {
    totalNodes: 0,
    graphNodes: 0,
    completionTime: 0,
};

/* function that processes the generated maze image into a multi dimensional array */
function processMaze(maze) {
    const arr = [];
};

/* function that draws the solution onto the map */
function drawSolution(graph, maze) {
    const newImage = null;
};

/* function that takes the paths of the maze and graphs the important decisions */
function graphMaze(arr) {
    const graph = {};
};

/* function that solves the maze using BFS */
function solveBFS(graph) {
    const solution = {};
};

/* function that solves the maze using DFS */
funtion solveDFS(graph) {
    const solution = {};
};

/* function that solves the maze using disjktras */
function solveDikjstras(graph) {
    const solution = {};

};
